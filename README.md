# JulyUnity
<p>Basically scooping out the contents of pre-existing unity project and placing it in same file as gitignore. 
When tweeking the screen size, discovered different scenes are reacting diffrently it git than in unity console. 
Perhaps an overview of individual scene stages is in order.</p>
