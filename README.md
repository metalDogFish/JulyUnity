# JulyUnity
Basically scooping out the contents of pre-exsisting unity project and placing it in same file as .gitignore, tweeking the screen size, but different scenes are reacting diffrently it git than in unity console. Perhaps an overhaul of scene stages using a static size.
